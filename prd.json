{
  "title": "Seller Management System – Mandatory shadcn/ui Refactor",
  "description": "This PRD defines a mandatory UI refactor for the existing internal Seller Management System. All existing functionality is correct and must remain unchanged. The sole objective is to replace all existing UI elements with shadcn/ui components and achieve a consistent, visually polished internal interface.",
  "scope": "ui-only",
  "ui_framework": {
    "name": "shadcn/ui",
    "usage_policy": "mandatory"
  },
  "hard_rules": [
    "All buttons must use shadcn/ui Button component",
    "All forms must use shadcn/ui Form system",
    "All inputs must use shadcn/ui Input, Select, or Textarea components",
    "All tables and lists must use shadcn/ui Table components",
    "All dialogs, modals, and popovers must use shadcn/ui Dialog, Sheet, or Popover components",
    "Native HTML elements (button, input, select, textarea, table) must not be used directly in the UI",
    "Custom-styled components are not allowed unless wrapped around shadcn/ui primitives"
  ],
  "ui_refactor_targets": [
    {
      "screen": "Seller List",
      "status": "complete",
      "required_components": ["Table", "Button", "Badge"],
      "requirements": [
        "Replace existing seller list UI with shadcn/ui Table",
        "Use Badge for seller status or tags",
        "Use Button for all row actions"
      ]
    },
    {
      "screen": "Seller Profile",
      "status": "complete",
      "required_components": ["Card", "Separator", "Button"],
      "requirements": [
        "Group profile sections using Card",
        "Separate sections with Separator",
        "Ensure consistent spacing and hierarchy"
      ]
    },
    {
      "screen": "Document Section",
      "status": "complete",
      "required_components": ["Table", "Badge", "Button"],
      "requirements": [
        "Display documents using Table",
        "Render document tags as Badge",
        "Render document links using Button or Link variant"
      ]
    },
    {
      "screen": "Forms (All Screens)",
      "status": "complete",
      "required_components": [
        "Form",
        "FormField",
        "FormItem",
        "FormLabel",
        "FormControl",
        "FormMessage",
        "Input",
        "Select",
        "Textarea",
        "Button"
      ],
      "requirements": [
        "All forms must use shadcn/ui Form primitives",
        "Validation errors must use FormMessage",
        "Optional fields must be visually indicated"
      ]
    },
    {
      "screen": "Notes & Service Field",
      "status": "complete",
      "required_components": ["Textarea", "Card"],
      "requirements": [
        "Notes must be placed inside Card",
        "Textarea must be used for all long-text input"
      ]
    },
    {
      "screen": "Feedback & States",
      "status": "complete",
      "required_components": ["Alert", "Skeleton", "Toast"],
      "requirements": [
        "Loading states must use Skeleton",
        "Errors must use Alert",
        "Success actions must trigger Toast"
      ],
      "notes": "Login page now uses Alert component for error messages"
    }
  ],
  "visual_standards": [
    "Consistent padding and spacing using Tailwind utility classes recommended by shadcn",
    "Neutral color palette with subtle accents",
    "Clear typography hierarchy for headings, labels, and values",
    "Hover and focus states must be visually clear and consistent"
  ],
  "enforcement": {
    "code_review_rule": "PRs introducing native HTML UI elements must be rejected",
    "acceptance_rule": "UI changes are accepted only if all interactive elements are shadcn/ui-based"
  },
  "non_goals": [
    "Changing business logic",
    "Adding new workflows",
    "Backend or database modifications"
  ],
  "success_criteria": [
    "No native HTML UI elements are used directly in the UI",
    "All screens visually conform to shadcn/ui design patterns",
    "UI is visibly cleaner and more consistent than the previous version",
    "Functional behavior remains unchanged"
  ]
}
